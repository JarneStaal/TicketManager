<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:TicketManager.Models;assembly=TicketManager"
             x:Class="TicketManager.Views.Misc.ProblemInfoPage"
             x:DataType="models:Problem" Title="{Binding Issue}">
    <ContentPage.Content>
        <StackLayout Margin="20">
            <Label FontSize="20" FontFamily="Bold" FontAttributes="Bold" Text="{Binding Issue}" TextColor="#0DA687"></Label>
            <Label TextColor="Black" Margin="0,20,0,0" Text="{Binding Solution}"></Label>
            <Button Text="Helpt dit niet?
                    Maak een ticket aan."></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>